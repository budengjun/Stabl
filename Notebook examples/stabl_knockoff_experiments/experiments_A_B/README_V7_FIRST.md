# Start here: V7

The primary next experiment is:

```text
run_experiment_b_completion_replacement.py
```

It tests median versus BayesianRidge posterior imputation on real omics X while
keeping the original STABL `stabl_min` procedure unchanged.

Run:

```bash
python -u experiments_A_B/test_v7_updates.py
```

Then follow:

```text
RUN_V7_REAL_X_POSTERIOR_REPLACEMENT.md
```
